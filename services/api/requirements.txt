# ── Web framework ──────────────────────────────────────────────────────────────
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0

# ── Pydantic ───────────────────────────────────────────────────────────────────
pydantic>=2.10.0,<3.0.0
pydantic-settings>=2.7.0,<3.0.0
email-validator>=2.2.0,<3.0.0

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.36,<3.0.0
# psycopg3 async driver — use postgresql+psycopg:// DSN (not python-jose CVE risk)
psycopg[binary]>=3.2.0,<4.0.0
alembic>=1.14.0,<2.0.0
boto3>=1.36.0,<2.0.0

# ── Auth — PyJWT replaces python-jose (CVE-2024-33663 avoided) ────────────────
PyJWT>=2.10.0,<3.0.0
cryptography>=44.0.0,<46.0.0
passlib[bcrypt]>=1.7.4,<2.0.0

# ── HTTP / file uploads ────────────────────────────────────────────────────────
httpx>=0.28.0,<1.0.0
python-multipart>=0.0.17,<1.0.0

# ── Queue / Cache ──────────────────────────────────────────────────────────────
redis>=5.2.0,<6.0.0
celery[redis]>=5.4.0,<6.0.0

# ── Logging ────────────────────────────────────────────────────────────────────
structlog>=24.4.0,<26.0.0

# ── Testing ────────────────────────────────────────────────────────────────────
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0
anyio>=4.7.0,<5.0.0
aiosqlite>=0.21.0,<1.0.0

# ── Linting / Type checking ────────────────────────────────────────────────────
ruff>=0.8.0,<1.0.0
mypy>=1.13.0,<2.0.0

# ── Sprint 4/5: parsing, embeddings, vector store ─────────────────────────────
qdrant-client>=1.12.0,<2.0.0
pymupdf>=1.25.0,<2.0.0
python-docx>=1.1.0,<2.0.0
# pillow>=11.0.0,<12.0.0
# pytesseract>=0.3.13,<1.0.0
