# Mobile visual proof — Sprint 99

The Playwright mobile regression test writes the Android/Chromium and iOS/WebKit proof captures into this directory.

The captures cover the bureau receipt declaration screen and the treasurer validation queue at a 390 × 844 mobile viewport. The test also asserts that the receipt screen has no horizontal overflow.
